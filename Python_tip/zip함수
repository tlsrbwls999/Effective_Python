arr1=[1,2,3,4,5]
arr2=['a','b','c','d','e']

for i,j in zip(arr1,arr2):
  print(i,j)
  
  
  #두배열을 비교하거나 합치거나 여튼 사용할때 그냥 
  zip함수 사용하는 것 떠올리기 
  두배열의 크기가 같다고 사용할 수 있는 것은 XXX
  두배열 크기가 상이하다면 작은 것에 맞추어서 나옴
  예를들어 arr1=[1,2,3]이면
  출력은 1,a 2,b 3,c까지만 나옴
